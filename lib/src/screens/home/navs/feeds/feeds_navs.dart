import 'package:flutter/material.dart';

class FeedsNav extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Feed'),
      ),
    );
  }
}